import java.util.*;
/**
 *
 * @author Muqadas 
 */
class Employee {
    private String name;
    private double salary;

   
    public Employee(String name, double salary) {
        this.name = name;
        this.salary = salary;
    }

    
    public void display() {
        System.out.println("Name: " + name + ", Salary: " + salary);
    }

    
    public String getName() {
        return name;
    }
}
public class EmployeeManagement {

    public static void main(String[] args) {

        Scanner s = new Scanner(System.in);
        ArrayList<Employee> e = new ArrayList<>();

        
        e.add(new Employee("Alice", 50000));
        e(new Employee("Bob", 60000));
        e(new Employee("Charlie", 55000));

        
        System.out.println("Employees List (Before Removal):");
        displayEmployees(e);

        
        System.out.print("\nEnter the name of the employee to remove: ");
        String nameToRemove = s.nextLine();
        removeEmployees(e, nameToRemove);

        
        System.out.println("\nEmployees List (After Removal):");
        displayEmployee(e);

        scanner.close();
    }

    public static void displayEmployees(ArrayList<Employee> e){
        if (e.isEmpty()) {
            System.out.println("No employees in the list.");
        } else {
            for (Employee emp : e){
                emp.display();
            }
        }
    }

   
    public static void removeEmployee(ArrayList<Employee> e,String name) {
        boolean found = false;
        for (int i = 0; i < e.size(); i++) {
            if (e.get(i).getName().equalsIgnoreCase(name)) {
                employees.remove(i);
                found = true;
                System.out.println("Employee '" + name + "' removed successfully.");
                break;
            }
        }
        if (!found) {
            System.out.println("Employee not found.");
        }
    }
}
